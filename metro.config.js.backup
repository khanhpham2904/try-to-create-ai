const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

// Minimal configuration for React Native 0.76+
config.resolver.platforms = ['ios', 'android', 'native', 'web'];

module.exports = config;
